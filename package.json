{
  "name": "@microservices-boilerplate/root",
  "type": "module",
  "workspaces": [
    "apps/**/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "clean": "turbo run clean --parallel",
    "fclean": "turbo run fclean --parallel && rm -rf node_modules .turbo bun.lockb"
  },
  "packageManager": "bun@1.1.30",
  "dependencies": {
    "turbo": "^2.1.3"
  }
}